import 'package:flutter/material.dart';

class SuccussScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Succussfull'),
      ),
      body: Center(
        child: Text('SuccussFulll'),
      ),
    );
  }
}
